export const ValidateField = (fields: any) => {
    if (fields) {
        console.log("fields", fields)
        // fields.map((field: any) =>
        //     console.log("field", field)
        // )
    }
}